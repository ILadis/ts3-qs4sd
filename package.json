{
  "name": "ts3-qs4sd",
  "version": "1.0.0-dev",
  "description": "A TeamSpeak 3 client plugin that integrates TeamSpeak 3 into Steam Deck's quick access menu.",
  "type": "module",
  "scripts": {
    "build": "rm -rf dist/ && rollup -c",
    "package": "rm -rf plugin.zip && cd decky/plugins && zip -r ../../plugin.zip ts3-qs4sd"
  },
  "dependencies": {
    "decky-frontend-lib": "3.x",
    "react": "16.x"
  },
  "devDependencies": {
    "rollup": "3.x"
  }
}
